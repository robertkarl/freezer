Freezer allows peers to share encrypted content via a static shared folder.

- public key crypto is used for contents, so sysadmins or third parties can't snoop at what is shared
- The static share has no server, so there is no entity to be shut down like other torrenting sites.
